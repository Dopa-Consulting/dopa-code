{
  "name": "@dopa-code/agent-runtime",
  "version": "0.2.0",
  "private": true,
  "description": "Entorno aislado para OpenCode CLI y bridge HTTP dentro de Dopa Code",
  "scripts": {
    "bridge": "bun bridge.js",
    "setup": "bun install --cwd opencode && bun install",
    "agent": "echo 'OpenCode CLI via bridge en localhost:4097'"
  },
  "engines": {
    "node": ">=20.0.0"
  }
}
